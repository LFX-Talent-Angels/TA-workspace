---
name: ta-yourskill
description: One sentence — what this skill does AND when an agent should use it (the trigger phrase or situation).
---

# /ta-yourskill

One or two sentences: what this workflow accomplishes and when to reach for it.

## Before you start

- Any preconditions (e.g. "must be on a branch", "tests must exist").

## Steps

1. First step — with the concrete command(s) for this repo.
2. Second step.
3. …

## Rules

- Constraints that always apply (DCO sign-off, no secrets, no `Mentees/` data…).

## Non-Claude agents

How a non-Claude agent should follow this as workflow intent (usually: "run the
equivalent steps with your own tooling — there's no special runtime").
